/*
 * ============================================================================
 * Problem  : <Problem Name>
 * LeetCode : https://leetcode.com/problems/<slug>/
 * NeetCode : <Category>
 * Difficulty: Easy | Medium | Hard
 * ============================================================================
 *
 * ━━━ APPROACH 1: BRUTE FORCE ━━━
 * <Describe the naive approach>
 *
 * Time:  O(?)
 * Space: O(?)
 * Why it works but isn't great: <reason>
 *
 * ━━━ APPROACH 2: OPTIMIZED ━━━
 * <Describe the key insight that improves on brute force>
 *
 * Time:  O(?)
 * Space: O(?)
 * Key insight: <what changes>
 *
 * ━━━ APPROACH 3: BEST / OPTIMAL ━━━
 * <Describe the optimal approach>
 *
 * Time:  O(?)
 * Space: O(?)
 * Why this is optimal: <lower bound argument or proof>
 *
 * ━━━ PRODUCTION CONNECTION ━━━
 * Pattern   : <Pattern Name>
 * Real-world: <Where this shows up in production systems>
 *
 * ━━━ GOTCHAS ━━━
 * - <Edge case or tricky part>
 * - <Common interview follow-up>
 * ============================================================================
 */

import java.util.*;

public class SolutionTemplate {

    // ━━━ APPROACH 1: BRUTE FORCE ━━━
    public static int solveBrute(int[] nums) {
        // TODO
        return -1;
    }

    // ━━━ APPROACH 2: OPTIMIZED ━━━
    public static int solveOptimized(int[] nums) {
        // TODO
        return -1;
    }

    // ━━━ APPROACH 3: BEST / OPTIMAL ━━━
    public static int solveBest(int[] nums) {
        // TODO
        return -1;
    }

    // ━━━ TEST CASES ━━━
    public static void main(String[] args) {
        // Test Brute Force
        // assert solveBrute(new int[]{...}) == expected;

        // Test Optimized
        // assert solveOptimized(new int[]{...}) == expected;

        // Test Best
        // assert solveBest(new int[]{...}) == expected;

        System.out.println("✅ All tests passed");
    }
}
